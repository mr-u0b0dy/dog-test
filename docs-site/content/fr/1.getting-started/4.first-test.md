---
title: Premier test
description: Créer et exécuter un test minimal avec dog-test.
navigation:
  icon: i-lucide-pen-line
---

# Premier test

Ce guide montre le flux minimal pour écrire un test C et l’exécuter.

## Exemple

```c
#include "dogtest/test_case.h"
#include "dogtest/assert.h"

DT_TEST("addition_works", DT_RESET_NONE, NULL, addition_works) {
  DT_ASSERT_EQ(2 + 2, 4);
  return DT_TEST_PASSED;
}

DT_MAIN()
```

## Exécution

```bash [Terminal]
cmake --preset host-debug
cmake --build --preset host-debug
ctest --preset host-debug
```

Ensuite, consultez la page [Assertions](/fr/essentials/assertions).
