---
title: Dog Test ワークフロー
description: dt_runner.py の実行フローと主要オプション。
---

# Dog Test ワークフロー

## 単体実行

```bash [Terminal]
python3 tools/dt_runner.py \
  --firmware build/arm-gcc-debug/examples/uart_loopback/uart_loopback \
  --backend pyocd \
  --serial-port /dev/ttyACM0 \
  --reset-mode soft
```

## テストプラン実行

```bash [Terminal]
python3 tools/dt_runner.py --test-plan tests/test_plan_smoke.json --junit-xml results.xml
```

## 主要オプション

- `--backend {pyocd,openocd}`
- `--retries N`
- `--junit-xml PATH`
- `--tag TAG` / `--filter STRING`

CI 連携時は CTest から同じランナーを呼び出せます。
