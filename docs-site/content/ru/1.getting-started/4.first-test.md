---
title: Первый тест
description: Создание минимального теста на dog-test.
navigation:
  icon: i-lucide-pen-line
---

# Первый тест

Ниже показан минимальный C-тест с одной проверкой.

## 1. Исходник теста

```c
#include "dogtest/test_case.h"
#include "dogtest/assert.h"

DT_TEST("addition_works", DT_RESET_NONE, NULL, addition_works) {
  DT_ASSERT_EQ(2 + 2, 4);
  return DT_TEST_PASSED;
}

DT_MAIN()
```

## 2. Сборка и запуск

```bash [Terminal]
cmake --preset host-debug
cmake --build --preset host-debug
ctest --preset host-debug
```

После этого можно перейти к [Assertions](/ru/essentials/assertions).
